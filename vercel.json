{
  "buildCommand": "cd chatapp && npm install && npm run build",
  "outputDirectory": "chatapp/dist",
  "rewrites": [
    {
      "source": "/socket.io/(.*)",
      "destination": "/api/socket.io"
    },
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
} 